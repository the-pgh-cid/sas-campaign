[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "sas-campaign"
version = "0.3.0"
description = "SAS migration plans, typed stateful execution, and verification receipts"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[project.optional-dependencies]
tui = ["rich>=13,<14"]
verify = ["numpy>=2,<3", "pandas>=3,<4", "scipy>=1.15,<2", "duckdb>=1,<2", "jsonschema>=4,<5"]

[project.scripts]
sas_campaign = "sas_campaign.cli:main"

[tool.setuptools]
packages = ["sas_campaign"]
py-modules = ["sas_semantics"]
